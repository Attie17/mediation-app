# Frontend environment example
VITE_SUPABASE_URL=http://localhost:54321/functions/v1 (replace with real)
VITE_SUPABASE_ANON_KEY=replace-with-anon-key
# Optional: API base (omit to use same origin via relative paths)
VITE_API_BASE=http://localhost:4000
