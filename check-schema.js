import pg from 'pg'; import dotenv from 'dotenv'; dotenv.config(); const pool = new pg.Pool({connectionString: process.env.DATABASE_URL}); (async () => { const r = await pool.query('SELECT column_name, data_type, udt_name FROM information_schema.columns WHERE table_name = ''uploads'' AND column_name IN (''user_id'', ''id'', ''case_id'') ORDER BY column_name'); console.log('uploads table:'); r.rows.forEach(c => console.log(  \: \ (\))); const r2 = await pool.query('SELECT column_name, data_type, udt_name FROM information_schema.columns WHERE table_name = ''app_users'' AND column_name = ''user_id'''); console.log('app_users table:'); r2.rows.forEach(c => console.log(  \: \ (\))); await pool.end(); })();
